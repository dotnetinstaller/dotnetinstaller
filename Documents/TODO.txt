-Verificare e provare nuova traduzione in tedesco
-Change the fieldname value of the Installed Check for MDAC 2.8 from Version to FullInstallVer, vedi mail di INVALID_HANDLE_VALUE (grnxnm@hotmail.com)
-Quando si creano le directory, ad esempio nella funzione StartDownload, quando si creano le directory usando CreateDirectory non vengono create le direcoty innestate (es. C:\temp\Temp1\Temp2)
-Dare la possibilità di eliminare la cache una volta eseguiti i componenti (anche il file di configurazione xml).
 O usare anche "You can implement IBindStatusCallback interface and set BINDF_PRAGMA_NO_CACHE flag in the call to IBindStatusCallback::GetBindInfo"
 Vedi anche http://groups-beta.google.com/group/microsoft.public.inetsdk.programming.webbrowser_ctl/messages/4d88fad0a744a409,e8ea3ca56cd445eb?thread_id=25c4b9c213b3fd33&mode=thread&noheader=1&q=URLDownloadToFile+cache#doc_4d88fad0a744a409
-Vedi email con estensioni di Matthias.Jentsch@gef.de

SECONDARI
-Inserire anche PROGRAMPATH come variabile
-Come si potrebbe gestire l'aggiornamento tramite Windows Update ? Con un componente OpenFile?
-Problema nel caso alcuni setup vengano installati "Solo per l'utente corrente". Forse basta prevedere un tipo di check a livello di HKEY_CURRENT_USER invece che su HKEY_LOCAL_MACHINE (anche perchè alcuni setup non hanno bisogno dei permessi di amministratore).
-Gestire il caso in cui i file da lanciare sono compressi in un CAB/ZIP (dare la possibilità nell'editor di comprimerli e cose simili)
-Is there a way of checking whether the user has administrative privileges on the machine? At two customer sites I have had trouble when installer tries to install the .NET framework and the user doesn't have privileges to do this. 
-Only allow one instance of the dotNetInstaller.exe open at once.
-Pensare se si riesce facilmente a far comparire un messaggio di errore che segnali all'utente che un determinato componente non può essere eseguito perchè richiede un requisito (che può essere un altro componente o ad esempio una lingua del sistema operativo particolare o un sistema operativo particolare)

-Aggiungere delle path constants (magari anche un sistema per leggere dalle variabili di sistema) e anche "Program Files"
-Aggiungere Installation CHeck come ad esempio il la verifica della presenza di una chiave di registry
-Aggiungere anche un modo per lanciare un componente se i check restituiscono true, ad esempio lanciare un componente solo se è presente una determinata chiave del registry. ( o se non è presente)